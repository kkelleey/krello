<div class="row-fluid">
  <div class="col-md-2">
    <ul class="nav nav-pills">
      {{#each model as |board|}}
        <li>
          {{#link-to 'boards.board' board}}
            {{board.title}}
          {{/link-to}}
        </li>
      {{/each}}
    </ul>
  </div>
  <div class="col-md-10">
    {{outlet}}
  </div>
</div>
